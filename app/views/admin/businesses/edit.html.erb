<h2><%= @business.name %></h2>

<%= form_for(@business, :url => admin_business_path ) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <div class="actions">
    <%= f.submit "Update", :disable_with => 'Saving...' %>
  </div>
<% end %>